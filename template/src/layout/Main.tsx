import React, { ReactElement } from 'react';

const Main = ({ children }: any): ReactElement => {
  return (
    <section className="main-container">
      {/* layout */}
    </section>
  );
};

export default Main;
